{
  "name": "02-weather",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "build": "bun build --compile src/index.ts --outfile out/weather.exe",
    "start": "bun run src/index.ts",
    "dev": "bun run src/index.ts --watch"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}